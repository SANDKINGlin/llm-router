# prometheus.yml — llm-router 监控抓取配置
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "llm-router"
    metrics_path: /metrics
    static_configs:
      - targets: ["llm-router:8789"]
    scrape_interval: 15s
