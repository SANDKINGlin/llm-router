# llm-router 环境变量样板 — 复制为 .env 填真值(.env 已 gitignore,不进 git/image)。
# 填哪个变量,对应 mnfst/providers.yaml 里引用它的 provider 就入候选池。
# 留空 = 该 provider 跳过(不崩),mock 始终兜底。
#
# 风控红线(脏 IP 环境,2026-06-22 实测):
#   ✅ NVIDIA NIM(脏IP不风控,免费档专属速率,主力推荐)
#   ✅ OpenRouter(聚合器宽容,但零额度账号高峰 429)
#   ⛔ Groq/Together/Fireworks(海外直连,脏IP易风控)
GROQ_API_KEY=
NVIDIA_API_KEY=
OPENROUTER_API_KEY=

