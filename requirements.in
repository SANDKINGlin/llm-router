# requirements.in — P8(供应链)锁依赖源。pip-compile → requirements.txt + hash。
# Phase1 核心运行时
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pyyaml>=6.0
aiosqlite>=0.20
# Phase1 provider 适配:P8 决策 defer 到 S2.x 接真 provider 时启用
# 理由:litellm 拉 openai/anthropic/cohere 等全家桶(上百传递依赖 + 海量 hash);
#       Phase1 用 MockProvider(src 零 import litellm,已验),S2.x 落地时去注释重锁即可。
# ⚠️ S2.x 启用时:先跑 `uv pip uninstall litellm`(若有手装残留)或用干净 venv,
#    再去注释 + 重跑 scripts/hash_lock.sh,避免手装版本与新锁冲突。
# litellm>=1.40
# 测试/开发
pytest>=8.0
httpx>=0.27
# Phase2 能力匹配(Phase2 启用时去注释)
# sentence-transformers>=2.7
